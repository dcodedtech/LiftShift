import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'Lyfta API — Import Workouts into LiftShift for Free Analytics',
  description:
    'Connect your Lyfta account via API key and get muscle heatmaps, plateau detection, PR tracking, and set-by-set feedback in LiftShift. Free, open source, runs in your browser.',
} satisfies Config;
