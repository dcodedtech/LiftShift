import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'Lyfta Workout Analytics — API Sync &amp; CSV Import Guide | LiftShift',
  description:
    'Import your Lyfta workout data via API key or CSV into LiftShift. Get muscle heatmaps, plateau detection, PR tracking, and set-by-set feedback — all analytics run locally in your browser.',
} satisfies Config;
