import type { Config } from 'vike/types';

export default {
  prerender: true,
  title: 'Strong vs LiftShift — Why Your Workout Logger Needs Analytics | Free &amp; Open Source',
  description:
    'Strong vs LiftShift: Strong is the most intuitive workout logger, but its analytics are basic and development has stalled. LiftShift is a free, open source analytics add-on that fills the gaps Strong leaves behind.',
} satisfies Config;
